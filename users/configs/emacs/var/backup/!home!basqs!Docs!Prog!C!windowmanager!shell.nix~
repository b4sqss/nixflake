{ pkgs ? import <nixpkgs> {} }:
pkgs.mkShell {
name = "wm shell";
buildInputs = with pkgs; [
  xorg.libX11
];
};

  RUST_SRC_PATH = "${pkgs.rust.packages.stable.rustPlatform.rustLibSrc}";

  shellHook = ''
  echo Welcome to mywm
  '';
}
